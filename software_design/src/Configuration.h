#ifndef _CONFIGURATION_H
#define _CONFIGURATION_H

// pin configuration

// how many pixels we have
// the ring has 24 but we're using the 0 as a sacrifice
#define RINGLED_NUM_LEDS 25

// wifi debug
// comment out to disable
// #define WIFI_DEBUG

#endif #_CONFIGURATION_H